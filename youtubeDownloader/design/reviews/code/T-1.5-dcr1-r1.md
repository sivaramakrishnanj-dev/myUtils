---
review_id: T-1.5-dcr1-r1
reviewed_commit: fd1b98b
reviewer: specDrivenReviewer
author: specDrivenImplementer + specDrivenTester
phase: code
task: T-1.5 (DCR-1 code resumption)
status: resolved
round: 1
---

# Review: T-1.5 — DCR-1 code resumption (NFR-ANDROID-* bump) — round 1

**Scope:** Code changes after DCR-1 amended the NFR-ANDROID-* triplet (design commits 9501351 + fd1b98b). Production files: `HttpConstants.java` (new), `InnerTubeClient.java`, `CaptionDownloader.java`, `ThumbnailDownloader.java`, `StreamDownloader.java`. Test files: `HttpConstantsTest.java` (new), `InnerTubeClientRequestShapeTest.java`, `InnerTubeClientTest.java`, `CaptionDownloaderBehaviorTest.java`, fixture `innertube-request-happy.json`.
**Inherited rulebook:** java-code-reviewer + _shared/review-rubric.md
**Added layer:** Spec-Adherence against NFR-ANDROID-CLIENT-VERSION, NFR-ANDROID-USER-AGENT, NFR-ANDROID-SDK-VERSION, AC-12.1, AC-12.2.
**Overlays applied:** (none).

---

## C1 — Shared `HttpConstants.ANDROID_USER_AGENT` extraction is clean and correct

- **Location:** `HttpConstants.java` (new class)
- **Severity:** Praise
- **Category:** Reuse
- **Spec citation:** NFR-ANDROID-USER-AGENT, AC-12.2
- **Reviewer comment:**
  Good extraction. A single source of truth for the User-Agent string, referenced by all four HTTP boundary components (`InnerTubeClient`, `CaptionDownloader`, `ThumbnailDownloader`, `StreamDownloader`). Package-private visibility is correct — the constant is an implementation detail of `yt-core`, not a public API. Private constructor prevents instantiation. Clean Code SRP.

---

## C2 — NFR triplet values match the amended spec exactly

- **Location:** `InnerTubeClient.java:49–53` (constants `CLIENT_VERSION`, `ANDROID_SDK_VERSION`, `OS_VERSION`)
- **Severity:** Praise
- **Category:** Spec-Adherence
- **Spec citation:** NFR-ANDROID-CLIENT-VERSION = `21.02.35`, NFR-ANDROID-SDK-VERSION = `30`, NFR-ANDROID-USER-AGENT = `com.google.android.youtube/21.02.35 (Linux; U; Android 11) gzip`
- **Reviewer comment:**
  All three constants match the DCR-1 amended values in `00-requirements.md` Group 2. The `osVersion = "11"` is consistent with SDK 30 (Android 11) and the User-Agent string. No mismatch between body fields and header values — the anti-abuse fingerprint-consistency requirement (US-12) is satisfied.

---

## C3 — No lingering references to old values

- **Location:** entire `yt-core/src/main/**` and `yt-core/src/test/**`
- **Severity:** Praise
- **Category:** Spec-Adherence
- **Spec citation:** DCR-1 bumped from `19.09.37` / SDK 34 / Android 14
- **Reviewer comment:**
  Grep confirms zero occurrences of `19.09.37`, `SDK 34`, or `Android 14` in production code, test code, and JSON fixtures. The migration is complete.

---

## C4 — Fixture `innertube-request-happy.json` matches the new constants

- **Location:** `yt-core/src/test/resources/fixtures/innertube-request-happy.json`
- **Severity:** Praise
- **Category:** Testing
- **Spec citation:** AC-12.1, `06-formal/innertube-player-request.schema.json`
- **Reviewer comment:**
  Fixture has `clientVersion: "21.02.35"`, `androidSdkVersion: 30`, `osVersion: "11"`. The `InnerTubeClientRequestShapeTest.fetchPlayer_bodyMatchesFixtureShape()` test asserts the live request body equals this fixture — strong regression guard against future drift.

---

## C5 — Test assertions updated consistently across all test classes

- **Location:** `InnerTubeClientRequestShapeTest.java`, `InnerTubeClientTest.java`, `CaptionDownloaderBehaviorTest.java`
- **Severity:** Praise
- **Category:** Testing
- **Spec citation:** NFR-ANDROID-USER-AGENT, NFR-ANDROID-CLIENT-VERSION, NFR-ANDROID-SDK-VERSION
- **Reviewer comment:**
  All assertion literals now use `21.02.35`, SDK `30`, and the updated User-Agent string. The `InnerTubeClientRequestShapeTest` covers 14 distinct assertions on the request shape — comprehensive coverage of AC-12.1 and AC-12.2. `CaptionDownloaderBehaviorTest.download_setsUserAgentToNfrAndroidUserAgent()` verifies the shared constant flows through to caption requests.

---

## C6 — `HttpConstantsTest` is minimal but sufficient

- **Location:** `HttpConstantsTest.java`
- **Severity:** Minor
- **Category:** Testing
- **Spec citation:** NFR-ANDROID-USER-AGENT
- **Reviewer comment:**
  The test asserts `isNotBlank()` and `startsWith("com.google.android.youtube/")` — a reasonable smoke test that the constant exists and has the expected prefix. It does not pin the exact value (that's done by `InnerTubeClientRequestShapeTest` and `CaptionDownloaderBehaviorTest`), which avoids triple-maintenance of the literal. Acceptable as-is.
- **Suggested fix:** None required. If the team later wants a single "golden value" assertion here, it can be added, but it's not a gap today given the other tests.

---

## C7 — `HttpConstants` visibility is package-private (correct)

- **Location:** `HttpConstants.java:7` — `final class HttpConstants`
- **Severity:** Praise
- **Category:** API-Design
- **Spec citation:** A Philosophy of Software Design — information hiding
- **Reviewer comment:**
  The class is package-private (`final class`, no `public`). All four consumers are in the same package. This prevents external callers from depending on the constant directly — if the value changes again (R-1 risk), only `yt-core` internals are affected. Good information hiding.

---

## Review summary

- Blockers: 0
- Majors: 0
- Minors: 1 (C6 — informational only, does not block)
- Nits: 0
- Praise: 5 (C1, C2, C3, C4, C5, C7)
- Spec-Adherence issues: 0
- Discussion: 0
- **Verdict:** resolved

## Coverage verification

- Task ACs satisfied in diff: AC-12.1 (clientVersion, androidSdkVersion, osVersion updated), AC-12.2 (User-Agent updated via shared constant)
- Task ACs not visibly satisfied: (none — all DCR-1 scope items addressed)
- NFRs satisfied: NFR-ANDROID-CLIENT-VERSION = `21.02.35` ✓, NFR-ANDROID-SDK-VERSION = `30` ✓, NFR-ANDROID-USER-AGENT = `com.google.android.youtube/21.02.35 (Linux; U; Android 11) gzip` ✓
- Contract tests satisfied in diff: CT-REQ-1 (videoId), CT-REQ-N7 (androidSdkVersion as integer) — both updated to new values
- Coverage on new class (`HttpConstants`): covered transitively by all four boundary-component test suites

## Inherited-rubric sweep

| Category | Findings |
|---|---|
| Correctness | None — constants match spec, no logic change |
| Testing | C6 (Minor, informational) |
| Readability | Clean — Javadoc on `HttpConstants` explains purpose |
| API-Design | C7 (Praise — correct visibility) |
| Reuse | C1 (Praise — DRY extraction) |

## Spec-adherence sweep

| Spec ID | Checked | Result |
|---|---|---|
| NFR-ANDROID-CLIENT-VERSION | ✓ | `21.02.35` in `InnerTubeClient.CLIENT_VERSION` and `HttpConstants.ANDROID_USER_AGENT` |
| NFR-ANDROID-SDK-VERSION | ✓ | `30` in `InnerTubeClient.ANDROID_SDK_VERSION` |
| NFR-ANDROID-USER-AGENT | ✓ | Exact match in `HttpConstants.ANDROID_USER_AGENT`, referenced by all 4 HTTP components |
| AC-12.1 | ✓ | Request body fields match amended NFR values |
| AC-12.2 | ✓ | User-Agent header set from shared constant |
| Old values purged | ✓ | No `19.09.37`, no SDK 34, no `Android 14` anywhere in src/main or src/test |
