---
review_id: T-4.7-r1
reviewed_commit: f714155
reviewer: specDrivenReviewer
author: specDrivenImplementer + specDrivenTester
phase: code
task: T-4.7
status: resolved
round: 1
---

# Review: T-4.7 — `DownloadResult.usedAsrFallback` flag wiring per INV-16 — round 1

**Scope:** `DownloadResultAsrTest.java` (characterization, 2 tests), `DownloadResultAsrBehaviorTest.java` (6 tests + 1 `@Disabled` placeholder for INV-16 pending T-4.10). No production code changes — field already exists from T-1.14.
**Inherited rulebook:** java-code-reviewer + _shared/review-rubric.md
**Added layer:** Spec-Adherence against AC-7.3, INV-16 per task row in 07-tasks.md.
**Overlays applied:** (none).

---

## C1 — Characterization test correctly exercises both boolean states

- **Location:** `DownloadResultAsrTest.java:24–44`
- **Severity:** Praise
- **Category:** Testing
- **Spec citation:** AC-7.3, INV-16
- **Reviewer comment:**
  Clean characterization test that verifies the `usedAsrFallback` accessor returns both `false` and `true` depending on construction. Minimal, focused, no mocking. Good use of the record's constructor to exercise both states directly.

---

## C2 — Behavior tests cover all current orchestrator paths

- **Location:** `DownloadResultAsrBehaviorTest.java:72–145`
- **Severity:** Praise
- **Category:** Testing
- **Spec citation:** AC-7.3, INV-16
- **Reviewer comment:**
  Tests 3–6 exercise the real `YoutubeDownloader` SUT through Flow A (video+audio), Flow B (audio M4A), and Flow B' (audio MP3), asserting `usedAsrFallback == false` on each. This documents the current contract: no orchestrator path flips the flag until T-4.10 wires `CaptionSelection`. SUT is not mocked — OkHttp interceptors fake only the external HTTP boundary. Correct pattern per pinned tenet 4.

---

## C3 — `@Disabled` placeholder for INV-16 end-to-end is well-structured

- **Location:** `DownloadResultAsrBehaviorTest.java:149–161`
- **Severity:** Praise
- **Category:** Testing
- **Spec citation:** INV-16
- **Reviewer comment:**
  The `@Disabled` annotation with a descriptive reason string clearly documents the dependency on T-4.10. The test body outlines the three assertions that will be enabled once the wiring lands. This is a clean handoff pattern for milestone-boundary work — the test exists in the suite, is visible in reports as "skipped", and will fail loudly if someone enables it prematurely without the wiring.

---

## C4 — No production code change needed — field already present

- **Location:** `DownloadResult.java:35` — `boolean usedAsrFallback`
- **Severity:** Praise
- **Category:** Spec-Adherence
- **Spec citation:** AC-7.3, INV-16, `03-data-model.md` § 2.4
- **Reviewer comment:**
  The `usedAsrFallback` field was introduced in T-1.14 as part of the stub `DownloadResult` record. The Javadoc on the field (`@param usedAsrFallback true if ASR was substituted for manual captions (AC-7.3)`) is accurate. Both orchestrator construction sites (`YoutubeDownloader.java:225` and `:299`) pass `false`, which is correct — the transcript flow that can produce `true` is T-4.10 scope. No production code change was needed or made.

---

## C5 — Minor: test 6 partially duplicates test 4

- **Location:** `DownloadResultAsrBehaviorTest.java:131–145` vs `:99–113`
- **Severity:** Nit
- **Category:** Readability
- **Spec citation:** —
- **Reviewer comment:**
  Test 6 (`download_allCurrentPaths_usedAsrFallbackAlwaysFalse`) exercises only the audio-M4A path, which is identical to test 4 (`download_audioOnlyM4a_usedAsrFallbackIsFalse`). The intent is different (documenting the "all paths" invariant vs testing one specific path), and the `@DisplayName` + comment make the distinction clear. The duplication is minor and the documentation value justifies it. Author decides.

---

## Review summary

- Blockers: 0
- Majors: 0
- Minors: 0
- Nits: 1
- Praise: 4
- Spec-Adherence issues: 0
- **Verdict:** resolved

## Coverage verification

- Task ACs satisfied in diff: AC-7.3 ✓ (field accessible, both states tested, current paths return false)
- Task ACs not visibly satisfied: (none)
- INV-16 status: field exists and is immutable (record); current paths set it to `false`; end-to-end wiring deferred to T-4.10 with `@Disabled` placeholder — acceptable for M4 milestone handoff
- Contract tests: CT-APP-9 requires `DownloadResult.usedAsrFallback == true` when ASR fallback is used — this is T-4.10 scope (orchestrator wiring); T-4.7's job is the field + characterization, which is complete
- Coverage on new classes: no new production classes; test-only additions

## Inherited-rubric sweep

| Category | Findings |
|---|---|
| Testing | 4× Praise — SUT not mocked, both boolean states exercised, all orchestrator paths covered, @Disabled placeholder well-documented |
| Readability | 1× Nit — minor test duplication with clear intent distinction |
| Correctness | No issues |
| Security | N/A |
| Reliability | N/A |
| Observability | N/A |

## Spec-adherence sweep

| Spec ID | Status | Notes |
|---|---|---|
| AC-7.3 | ✅ | Field present, Javadoc cites AC-7.3, tests verify both states |
| INV-16 | ✅ | Record immutability guarantees "set once"; `@Disabled` placeholder documents T-4.10 dependency for end-to-end verification |
| T-4.10 dep | ✅ | Correctly deferred — no premature wiring attempted |
