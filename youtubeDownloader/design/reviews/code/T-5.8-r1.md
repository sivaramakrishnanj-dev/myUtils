---
review_id: T-5.8-r1
reviewed_commit: a1ff421
reviewer: specDrivenReviewer
author: specDrivenImplementer + specDrivenTester
phase: code
task: T-5.8
status: resolved
round: 1
---

# Review: T-5.8 — v1.0.0 tag, GitHub Release with fat-jar artifact attached — round 1

**Scope:** `CHANGELOG.md` (new), `RELEASE_NOTES.md` (new), `RELEASING.md` (new) produced by specDrivenImplementer for task T-5.8.
**Inherited rulebook:** java-code-reviewer + _shared/review-rubric.md
**Added layer:** Spec-Adherence against `05-operations.md` § 6.2 (Future Work carve-out), `07-tasks.md` T-5.8 row.
**Overlays applied:** (none).

---

## C1 — CHANGELOG covers all six milestones with accurate commit SHAs

- **Location:** `CHANGELOG.md` (full file)
- **Severity:** Praise
- **Category:** Readability
- **Reviewer comment:**
  All six milestones (M0–M5) are represented with per-task commit SHAs. Spot-checked `3cdf7c7` (M0 boundary), `d7d8604` (M1), `7f1d239` (M2), `1bfc0ab` (M3), `51a10dd` (M4), `a1ff421` (M5) — all match the actual git log. The Keep-a-Changelog 1.1.0 format is correctly followed with `### Added` and `### Known Limitations` sections. The `[1.0.0]` link footer points to the correct GitHub releases URL.

## C2 — RELEASING.md `--version` output comment is slightly inaccurate

- **Location:** `RELEASING.md:12`
- **Severity:** Minor
- **Category:** Readability
- **Spec citation:** `05-operations.md` § 1.4 (build verification checklist)
- **Reviewer comment:**
  The inline comment says `# Should print: 1.0.0` but the actual output of `java -jar ... --version` is `youtube-downloader 1.0.0` (includes the program name, per picocli's `@Command(version = ...)` convention). A maintainer following the guide literally would think the output is wrong. Per *Clean Code* (Martin) — documentation should not mislead.
- **Suggested fix:**
  Change the comment to `# Should print: youtube-downloader 1.0.0`.

## C3 — Release ceremony order is correct and safe

- **Location:** `RELEASING.md` (full file)
- **Severity:** Praise
- **Category:** Reliability
- **Reviewer comment:**
  The ceremony follows the correct order: (1) verify build, (2) tag, (3) push tag, (4) create GitHub Release with artifact. Tag creation is deferred to the user (not automated by the implementer), respecting the destructive-action safety guardrail. The `gh release create` syntax (`v1.0.0 <file> --title --notes-file`) is the documented GitHub CLI pattern. The "Future releases" section provides a repeatable checklist for subsequent versions.

## C4 — RELEASE_NOTES features and limitations match the spec

- **Location:** `RELEASE_NOTES.md` (full file)
- **Severity:** Praise
- **Category:** Spec-Adherence
- **Spec citation:** AC-1.3, AC-2.1, AC-2.4, AC-6.1, AC-5.3, OOS-1, OOS-2, OOS-3, OOS-6
- **Reviewer comment:**
  The five headline features (video, audio M4A, MP3, transcript, thumbnail) map directly to US-1, US-2, US-6, and the thumbnail capability. The "Known limitations" table correctly cites exit codes 22 (cipher/OOS-2), 21 (live/OOS-3), 2 (playlists/OOS-1), 20 (age-restricted/OOS-6+OOS-7). The "Install" section's prerequisites (Java 17+, Maven 3.9+, ffmpeg ≥ 4.0) match `05-operations.md` § 1.1 exactly. The claim "All 78 contract tests pass" is consistent with `07-tasks.md` § 3.1 totals.

## C5 — RELEASE_NOTES install section references `mvn clean package` not `mvn clean verify`

- **Location:** `RELEASE_NOTES.md:18`
- **Severity:** Nit
- **Category:** Readability
- **Reviewer comment:**
  The install section uses `mvn clean package` which is fine for end-users (skips integration tests). However, `05-operations.md` § 1.2 documents `mvn clean package` as the user-facing build command and `mvn clean verify` as the full gate. The choice of `package` here is appropriate for a quickstart — no issue, just noting the deliberate difference.

## C6 — Spec tension: § 6.2 says "no GitHub Release binaries" but T-5.8 creates one

- **Location:** design spec — `05-operations.md` § 6.2
- **Severity:** Discussion
- **Category:** Discussion
- **Spec citation:** `05-operations.md` § 6.2, `07-tasks.md` T-5.8 row
- **Reviewer comment:**
  `05-operations.md` § 6.2 states: "No published Maven artifact, no Homebrew tap, no GitHub Release binaries. All three are Future Work." However, T-5.8 in `07-tasks.md` explicitly says "v1.0.0 tag, GitHub Release with fat-jar artifact attached" and references "§ 6.2 Future Work carve-out" — meaning the task intentionally overrides the spec's Future Work designation for the GitHub Release specifically. The code is correct against the task row. The spec itself has a stale sentence that should be updated to reflect this carve-out.
- **Suggested amendment kind:** ac-update (update `05-operations.md` § 6.2 to note that GitHub Release with fat-jar was promoted from Future Work to M5)
- **Impact on this review's verdict:** none — this is a spec gap, not a code defect.

## C7 — CHANGELOG "Known Limitations" section uses non-standard Keep-a-Changelog heading

- **Location:** `CHANGELOG.md:82`
- **Severity:** Nit
- **Category:** Readability
- **Reviewer comment:**
  Keep-a-Changelog 1.1.0 defines these section types: Added, Changed, Deprecated, Removed, Fixed, Security. "Known Limitations" is not one of them. This is a minor deviation from the format the file claims to follow. Pragmatically it's useful information and most parsers won't choke on it. Author decides.
- **Suggested fix:**
  Either rename to a prose paragraph outside the `### ` sections, or accept the deviation as intentional documentation.

---

## Review summary

- Blockers: 0
- Majors: 0
- Minors: 1
- Nits: 2
- Praise: 3
- Discussion: 1
- **Verdict:** resolved

## Coverage verification

- Task ACs satisfied in diff: `05-operations.md` § 6.2 (carve-out — GitHub Release prep artifacts delivered)
- Task ACs not visibly satisfied: none — T-5.8 is a documentation/release-prep task; the actual tag creation is deferred to the user (destructive action)
- Contract tests satisfied in diff: n/a (no code logic in this task)
- Contract tests not yet satisfied: n/a
- Coverage on new classes: n/a (no Java code in this task)

## Inherited-rubric sweep

No Java code in this diff — inherited `java-code-reviewer` rules do not apply. The three markdown files were reviewed for correctness, completeness, and spec alignment.

## Spec-adherence sweep

| Spec ref | Result |
|---|---|
| `05-operations.md` § 6.2 | Satisfied via carve-out; Discussion filed (C6) |
| `07-tasks.md` T-5.8 row | ✅ — CHANGELOG, RELEASE_NOTES, RELEASING all delivered; tag deferred to user |
| `05-operations.md` § 1.1 prerequisites | ✅ — RELEASE_NOTES install section matches |
| `05-operations.md` § 1.4 build verification | ✅ — RELEASING step 1 runs `mvn clean verify` |
| M5 verification gate "v1.0.0 tagged" | Deferred — tag creation is user's action per safety guardrails |
